---
import metaData from '../../../data/meta.json';
import VenueView from '../../../views/VenueView.astro';
import type { Meta } from '../../../lib/types.ts';

export function getStaticPaths() {
  const site = metaData as Meta;
  return site.venues.map((venue) => ({ params: { id: String(venue.venue_id) }, props: { venue } }));
}

const { venue } = Astro.props;
---

<VenueView lang="en" venue={venue} />
